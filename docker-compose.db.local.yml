# Local override: separate container + port so laptop data never mixes with production
services:
  mysql:
    container_name: viverider-mysql-local
    ports:
      - "3308:3306"
    volumes:
      - viverider_mysql_data_local:/var/lib/mysql

volumes:
  viverider_mysql_data_local:
