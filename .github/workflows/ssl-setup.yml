name: Setup SSL

# Disabled — nginx/SSL is managed on the server manually.
# Keep this file so old workflow links do not 404; it no-ops.

on:
  workflow_dispatch:

jobs:
  ssl:
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: |
          echo "SSL setup is disabled. Configure nginx yourself on the server."
          echo "Deploy does not overwrite nginx. IP + domain both work via your nginx + Laravel request host."
